{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/server"
        },
        {
            "source": "/auth/(.*)",
            "destination": "/server"
        }
    ]
}